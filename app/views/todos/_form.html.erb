<ul>
  <% if @todo.errors.each do |error,message| %>
    <li><%= "#{error.capitaliza} #{message}" %></li>
  <% end %>
</ul>

<div class="ui raised very padded text container segment"> 
  <h1 class="ui header">To Do List</h1> 
  </p>  
    <ul>
      <%= form_for @todo do |f| %>
        <div class="ui segment">
          <h2 class="ui right floated header">修改</h2>
          <div class="ui clearing divider"></div>
          <p><%= f.label :title, "Title" %></p>
          <div class="ui clearing divider"></div>
          <p><%= f.text_field :title %></p> 
          <div class="ui clearing divider"></div>            
          <p><%= f.label :date, "Date" %></p>
          <div class="ui clearing divider"></div>
          <p><%= f.text_field :date %></p>
          <div class="ui clearing divider"></div>
          <p><%= f.label :note, "Note" %></p> 
          <div class="ui clearing divider"></div>
          <p><%= f.text_area :note %></p>
          <p><%= f.submit %></p>    
        </div>
       <% end %>     
      <% end %>
    </ul>
 </div>   
