<div class="ui raised very padded text container segment"> 
  <h1 class="ui header">To Do List</h1> 
  <p align="right"><%= link_to 'Back to index',todos_path , class: "ui primary button" %>
  </p>  
    <ul>
          <div class="ui segment">
            <h2 class="ui right floated header">Mission - <%= @todo.title %></h2>
            <div class="ui clearing divider"></div>
            <p>Due date:<%= @todo.date %></p>
            <div class="ui clearing divider"></div>
            <p class="div1"> Note: <%= @todo.note%></p> 
          </div>
    </ul>
</div>   